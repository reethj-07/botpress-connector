services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      DATABASE_URL: sqlite:///./botpress_connector.db
      BOTPRESS_CHAT_BASE_URL: https://chat.botpress.cloud
      CORS_ORIGINS: http://localhost:8000
    ports:
      - "8000:8000"

